[tox]
envlist = py39, py310, py311, py312, py313
requires =
    tox>=4
    tox-uv

[testenv]
extras = test

commands =
    pytest -qx tests tests_modern
    behave --format progress --stop --tags=-wip
